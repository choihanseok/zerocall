# ZERO CALL TASK IMPACT REPORT / PLAN

- Task ID: ZC-TASK-20260910-003
- Objective: Password Credential Storage Foundation
- Module: AUTH / Service: COMMON / Risk: CRITICAL (credential persistence)
- Authorization: 기술 선택 위임 및 사용자의 다음 작업 자동 진행 요청.
- Before: clean branch, base 0244cce (PR #2). Account/Hash Foundation COMPLETED. Bootstrap READY_FOR_DEVELOPMENT=YES (LOCAL). 기존 테스트73개. Credential 저장 구현 없음.

## 기준 및 추적

00_MASTER_RULE, 16_CODEX_TASK_RULE, 17_PROJECT_BOOTSTRAP_CHECKLIST 및 관련 Account/Auth 요구·DB·보안·검수 항목을 검토했다. ZR-COM-ACCOUNT-003/004는 CONFIRMED이며 이번에는 그 저장 기반만 구현한다. 08_DATABASE_DEFINITION §7의 accounts.password_hash와 §99의 해시만 저장 원칙, 14_SECURITY_OPERATION §5/6/43를 따른다. 기존 원본 ID는 보존한다. TC-ACC-001/TC-AUTH-001 전체 완료가 아니다.

## 영향 및 구현계획

1. accounts에 nullable password_hash만 추가하는 Migration 20260910_003. 기존 행은 NULL이며 backfill/삭제/상태변경 없음. 파괴적 downgrade는 거부한다.
2. 일반 Account Domain과 조회에서 해시를 제외한다. ORM 컬럼은 deferred+raiseload로 우발적 로딩을 막는다.
3. AUTH 전용 Repository Protocol/SQLAlchemy 구현을 추가한다. 현재 Foundation에서 생성 가능한 PENDING 계정의 비어 있는 credential만 최초 저장하는 제한된 내부 연산이다. 동시 저장은 조건부 UPDATE로 한 건만 성공시킨다. 변경·재설정·로그인 허용 정책을 정의하지 않는다.
4. 내부 Service는 기존 HashAdapter를 재사용하고 저장 성공 후 계정 UUID와 이벤트만 로그에 남긴다. Repository는 검증된 해시만 받는다. 내부 조회는 SecretStr로 감싸서 반환하며 인증 성공 판정을 하지 않는다.
5. 실제 SQLite Migration 전후 데이터 보존, 재실행, 원자성·동시성·실패 rollback, 일반 조회 비노출, 비밀값 미출력과 기존 전체 회귀 테스트·lint·build를 검증한다.
6. DB/API/Security/Test/Requirement 문서 및 Task Report 동기화, 후속 Draft PR 게시.

영향 없음: Frontend, 공개 API, Account 상태전이, Role/Scope, 주문·결제·정산·알림·외부연동. 인증 API/가입 정책/비밀번호 변경·재설정/영구 Audit는 미구현이며 NEED_REVIEW 유지. 오류 시 이번 변경만 수정하고 기존 변경은 원복하지 않는다. 현재 Task 완료 후 종료한다.
