# ZERO CALL TASK PLAN

- Task: ZC-TASK-20260910-004 / Staging and Production Infrastructure
- Risk: CRITICAL / Module: PLATFORM / Service: COMMON
- Authorization: 운영·스테이징 환경 및 운영 DB 구축 요청. 서버/클라우드 계정·예산은 사용자에게 확인 중.
- Baseline: b7e18dc, clean working tree, 기존92 tests PASS. Bootstrap LOCAL 준비 완료. Docker/psql/클라우드 배포 설정 없음. 현재 SQLite 전용 설정은 운영 실행을 거부한다.

## Requirement / Impact

00/16/17 및 14_SECURITY_OPERATION의 환경분리(§115~118), TLS(§40~41), DB 계정(§52~53), 백업/복구(§99~106), 배포/Migration(§123~132) 기준을 따른다. 인프라 보충 요구 ID INFRA-004로 추적하며 원본 ID를 바꾸지 않는다.

서버 선택과 무관한 준비: PostgreSQL 연결 지원, 기존 SQLite 유지, PostgreSQL PK 오류 호환, DB 통합 테스트 및 Docker 배포 패키지/CI, 운영 체크리스트. Migration은 기존 세 개를 재사용하며 기존 데이터 이관/삭제는 하지 않는다. 운영/스테이징 PostgreSQL은 TLS verify-full 및 CA 설정을 요구한다. 서버 시작 시 자동 Migration은 하지 않는다.

실제 자원 생성은 계정/프로젝트/예산 확인 후 스테이징과 운영의 서버·DB·계정·Secret을 분리한다. HTTPS, private DB network, 최소권한 runtime/migration 계정, 백업·복구 시험, 모니터링, 실제 환경 smoke test가 끝나기 전 READY_FOR_PRODUCTION=YES로 표시하지 않는다. 서비스 업무 정책·새 로그인 API는 범위 밖이다.
